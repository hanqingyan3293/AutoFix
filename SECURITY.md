# 安全策略

## 支持范围

仅对最新发布版本提供安全修复。

| 版本 | 支持 |
| --- | --- |
| 1.0.x | 是 |
| < 1.0 | 否 |

## 如何报告安全问题

请**不要**通过公开 Issue 报告安全问题。请使用 GitHub 的
[私密漏洞报告](https://docs.github.com/code-security/security-advisories/guidance-on-reporting-and-writing/privately-reporting-a-security-vulnerability)
功能，或直接联系仓库维护者。

报告中请尽量包含：

- 受影响版本与目标系统
- 复现步骤（越具体越好）
- 影响评估
- 可行的修复建议（如有）

## 什么算安全问题

本工具以管理员权限运行并主动执行停止服务、结束进程、删除文件与注册表项、修改访问权限等操作。
因此以下几类问题都在受理范围内：

| 类别 | 示例 |
| --- | --- |
| **越权删除** | 因路径拼接、通配或判据不当，删除了 Autodesk 范围之外的文件或注册表项 |
| **路径穿越** | 特制输入使删除目标逃逸出预期目录 |
| **权限提升** | 程序被非管理员调用时仍能造成系统级改动 |
| **绕过确认** | 通过参数或状态构造，跳过破坏性操作的确认流程 |
| **预演模式失效** | 预演模式下仍发生了实际写入或删除 |
| **敏感信息泄漏** | 日志、导出文件中包含不应记录的信息 |
| **外部输入未校验** | 从文件名、注册表值、系统命令输出中读取的内容被直接用于路径或命令构造 |

## 设计上的安全约定

这些是维护者需要保持的性质，若发现某处被破坏，请按安全问题报告：

1. **预演模式必须覆盖全部破坏性操作** —— 开启后不得产生任何实际改动
2. **只读模块必须零写入** —— 环境检测、清理后校验、残留扫描
3. **破坏性操作默认取消** —— 确认框默认按钮为「取消」
4. **不可恢复操作要求手动输入 `YES`**
5. **共用组件默认保留** —— 影响其他软件的删除需用户显式勾选
6. **完全离线** —— 不发起网络请求

## 已知的固有风险（非漏洞）

以下属于工具的设计行为，不属于安全漏洞：

- 工具需要管理员权限，这是完成任务的前提
- 「错误 3」修复按名称特征判断目录，可能命中非 Autodesk 目录（已在界面与文档中警示）
- 「错误 4000 / -9」修复会清理 `SOFTWARE\Classes` 下的叶子键，影响面较广（已在界面中警示）
- 安全软件可能对停服务、改注册表权限的行为告警

